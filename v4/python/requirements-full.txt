# Cynapse Full Dependencies (All Features)
# Install with: pip install -r requirements-full.txt

# Include all core dependencies
-r requirements.txt

# TUI Enhancement
rich>=13.0.0            # Better terminal formatting (optional)

# AI/ML - Required for Elara model and training
torch>=2.0.0            # PyTorch for Elara inference
transformers>=4.30.0    # HuggingFace transformers
tiktoken>=0.5.0         # Token counting
sentencepiece>=0.1.99   # Text tokenization
protobuf>=3.20.0        # Protocol buffers

# LLM Backends (Choose at least one)
ollama>=0.1.0           # Local LLM via Ollama
# llama-cpp-python      # Alternative: Direct GGUF support

# OCR - Required for Owl neuron (document scanning)
pytesseract>=0.3.10     # OCR engine
Pillow>=10.0.0          # Image processing
pypdf>=3.0.0            # PDF parsing

# Audio - Required for Bat/Whistle authentication
pyaudio>=0.2.11         # Audio I/O
scipy>=1.10.0           # Signal processing

# Container - Required for Octopus neuron
aiodocker>=0.21.0       # Docker API client

# Progress bars
tqdm>=4.65.0            # Progress indicators

# Windows Terminal Colors
colorama>=0.4.6         # Windows ANSI color support
