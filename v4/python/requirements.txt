# Cynapse Core Dependencies (Minimal Runtime)
# Install with: pip install -r requirements.txt

# Core - Required for basic functionality
pyyaml>=6.0              # Configuration file parsing (YAML)
numpy>=1.24.0            # Vector operations in HiveMind (lazy loaded)

# Security - Optional but recommended
cryptography>=3.4.7      # Ed25519 signing in Elephant neuron (optional fallback)
blake3>=0.3.3           # Fast hashing (optional, falls back to hashlib)

# Networking
aiohttp>=3.8.0          # Async HTTP for Wolverine neuron

# System
psutil>=5.9.0           # System monitoring (optional, used if available)

# Build Tools (Development only)
# pyinstaller>=5.0      # Uncomment to build standalone executable
