## 2025-05-08 - Add placeholder hint to empty TUI input\n**Learning:** The TUI component rendered inputs in multiple places (both idle and active states), and resizing to a very small window could crash due to slice bound checks when parsing messages.\n**Action:** Add dim colored placeholder hints to all input states and proactively add defensive index checks for terminal resizing.
