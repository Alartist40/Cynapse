# Cynapse Core Dependencies (Minimal)
# For full installation with all AI features, use: pip install -r requirements-full.txt

# Core
numpy>=1.24.0
cryptography>=3.4.7
aiohttp>=3.8.0
psutil>=5.9.0
pyyaml>=6.0
colorama>=0.4.6
tqdm>=4.65.0

# Optional / Performance
blake3>=0.3.3

# TUI
# rich>=13.0.0  # Optional, TUI.py uses raw ANSI by default but recommended

# AI (Lazy Loaded)
# torch>=2.0.0
# Build Tools
pyinstaller>=5.0
