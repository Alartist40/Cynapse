# Model Storage Directory

This directory stores AI model data:
- Trained model checkpoints
- Quantized models (GGUF format)
- Fine-tuned weights

**Note**: Model files are typically large (GB+) and should NOT be committed to Git.
Add them to `.gitignore` or use Git LFS.
